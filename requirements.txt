# Core scientific stack used by the notebooks and run_saved_model.py.
# Versions are the ones I developed against; slightly newer minors should be fine.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0

# Only needed if you want to open / re-run the .ipynb files locally.
jupyter>=1.0
